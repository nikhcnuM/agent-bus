services:
  redis:
    image: redis:7-alpine
    container_name: agent-bus-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - agent-bus-redis-data:/data

volumes:
  agent-bus-redis-data:
